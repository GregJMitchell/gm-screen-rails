<div class='page-wrapper'>
  <div class='page-header-end'>
    <%= link_to '+ New', new_campaign_quest_path(@campaign), class: 'btn btn-primary' %>
  </div>
  <div class='single-card'>
      <%= render partial: 'shared/table', locals: {headers: @campaign.quests.klass.columns_hash.keys, rows: @campaign.quests, model: Quest} %>
  </div>
</div>