<div class='page-wrapper'>
  <div class='page-header-end'>
    <%= link_to '+ New', new_campaign_city_path(@campaign), class: 'btn btn-primary' %>
  </div>
  <div class='single-card'>
      <%= render partial: 'shared/table', locals: {headers: @campaign.cities.klass.columns_hash.keys, rows: @campaign.cities, model: City} %>
  </div>
</div>